<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    android:foreground="?attr/selectableItemBackground"
    app:cardCornerRadius="18dp"
    app:cardElevation="3dp"
    app:cardUseCompatPadding="true"
    app:cardBackgroundColor="@color/card_white">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp">

        <!-- Фото платья -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/cardImage"
            android:layout_width="96dp"
            android:layout_height="136dp"
            app:cardCornerRadius="14dp"
            app:cardElevation="0dp"
            app:strokeWidth="1dp"
            app:strokeColor="@color/chip_bg"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent">

            <ImageView
                android:id="@+id/img"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@color/bg_milk"
                android:scaleType="fitCenter"
                android:src="@drawable/ic_image_placeholder"
                android:contentDescription="Фото платья"/>
        </com.google.android.material.card.MaterialCardView>

        <!-- Кнопка избранного -->
        <ImageButton
            android:id="@+id/btnFav"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="В избранное"
            android:src="@drawable/ic_favorite_border"
            app:tint="@color/icon_rose"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Название -->
        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Свадебное платье миди из ткани плиссе"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@color/text_main"
            android:maxLines="2"
            android:ellipsize="end"
            app:layout_constraintStart_toEndOf="@id/cardImage"
            app:layout_constraintEnd_toStartOf="@id/btnFav"
            app:layout_constraintTop_toTopOf="@id/cardImage"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="8dp"/>

        <!-- Артикул -->
        <TextView
            android:id="@+id/tvSku"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Артикул: NK 3511"
            android:textSize="12sp"
            android:textColor="@color/text_muted"
            app:layout_constraintStart_toStartOf="@id/tvTitle"
            app:layout_constraintTop_toBottomOf="@id/tvTitle"
            app:layout_constraintEnd_toEndOf="@id/tvTitle"
            android:layout_marginTop="4dp"/>

        <!-- Цвет + стиль -->
        <TextView
            android:id="@+id/tvMeta"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Молочный • Минимализм"
            android:textSize="12sp"
            android:textColor="@color/text_muted"
            android:maxLines="2"
            android:ellipsize="end"
            app:layout_constraintStart_toStartOf="@id/tvTitle"
            app:layout_constraintTop_toBottomOf="@id/tvSku"
            app:layout_constraintEnd_toEndOf="@id/tvTitle"
            android:layout_marginTop="4dp"/>

        <!-- Цена -->
        <TextView
            android:id="@+id/tvPrice"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="58 000 сом"
            android:textSize="15sp"
            android:textStyle="bold"
            android:textColor="@color/accent_rose"
            app:layout_constraintStart_toStartOf="@id/tvTitle"
            app:layout_constraintTop_toBottomOf="@id/tvMeta"
            app:layout_constraintEnd_toEndOf="@id/tvTitle"
            android:layout_marginTop="10dp"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>
